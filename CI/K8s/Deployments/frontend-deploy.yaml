apiVersion:  apps/v1 
kind: Deployment
metadata:
  name: font-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: front
  template:
    metadata:
      name: app 
      labels:
        tier: front

    spec:
      containers:
        - name: frontend
          image: shebl22/frontend:915e45d1b58d07bb54a2d4847451ad6451c9ff4e
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: cm-front
